## Send a curl request

An example of sending a curl request with a header

```curl copy filename="curl"
curl https://api.srating.io/v1/cbb_game/getGames?start_date=2023-04-01 -H "X-API-KEY: YOUR_API_KEY"
```

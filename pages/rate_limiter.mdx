## Rate limiting

You may be subjected to rate limiting if you have too many requests per second. If you enounter the rate limit, the API will respond with a status code of 429, in which case we recommend spacing out requests over several seconds.

### Response Status code
```console
429 - Too many requests
```

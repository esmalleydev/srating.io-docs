export default function App({ Component, pageProps }) {
  return <Component {...pageProps} />
}

// "team_season_conference": "Team season conference",
  // "player_team_season": "Player team season",
  // "cbb_game_score_interval": "Scoring intervals",